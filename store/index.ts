// ============================================================
// STORE BARREL — re-exports all store hooks for convenience
// Usage: import { useStore } from '@/store'
// ============================================================
export { useStore } from './useStore'
export type { AppNotification } from './useStore'
